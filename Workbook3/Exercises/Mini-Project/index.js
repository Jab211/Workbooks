"use strict"

function init() {
    let submitButton = document.getElementById(submitButton);
    submitButton.onclick = submitButtonClicked;
}

window.onload = init;

function submitButtonClicked() {
    numberOfScoopsInput
    const basePrice = 2.25
    const sprinklesOption = 0.5
    
}